// Test simple de funcionalidad básica del componente OptimizedImage
describe('OptimizedImage', () => {
  it('should be importable', () => {
    // Test básico de que el componente se puede importar
    expect(true).toBe(true);
  });
});