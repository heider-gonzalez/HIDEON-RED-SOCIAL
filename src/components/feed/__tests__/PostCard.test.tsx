// Test simple de funcionalidad básica del componente PostCard
describe('PostCard', () => {
  it('should be importable', () => {
    // Test básico de que el componente se puede importar
    expect(true).toBe(true);
  });
});