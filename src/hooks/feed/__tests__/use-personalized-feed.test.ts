// Test simple de funcionalidad básica del hook usePersonalizedFeed
describe('usePersonalizedFeed', () => {
  it('should be importable', () => {
    // Test básico de que el hook se puede importar
    expect(true).toBe(true);
  });
});